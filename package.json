{
  "name": "hybrid-differentiable-defferred-splat-mesh-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:smoke": "node --test tests/smoke/*.test.mjs",
    "smoke:visual": "node scripts/run-visual-smoke.mjs",
    "smoke:visual:real": "node scripts/run-visual-smoke.mjs --require-real-splat"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69",
    "playwright-core": "^1.59.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
