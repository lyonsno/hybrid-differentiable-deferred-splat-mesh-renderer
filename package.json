{
  "name": "hybrid-differentiable-defferred-splat-mesh-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --experimental-strip-types --experimental-transform-types --test tests/*.test.ts && tsc --ignoreConfig --module NodeNext --moduleResolution NodeNext --target ES2022 --types @webgpu/types --skipLibCheck --outDir node_modules/.cache/renderer-tests --rootDir . src/splatPlateContract.ts && node --test tests/renderer/*.test.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69",
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
